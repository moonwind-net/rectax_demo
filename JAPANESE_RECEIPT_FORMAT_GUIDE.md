# 日本小票格式识别指南

## 概览

此文档记录了 RecTax OCR 系统对日本小票格式的完整支持，包括消費税（消费税）、税率、付款方式和找零等字段的检测规则。

---

## 1. 消費税（消费税）识别

### ✅ 支持的消費税关键词

系统可以识别以下消費税相关的关键词：

| 关键词 | 含义 | 备注 |
|--------|------|------|
| 消費税 | 消费税 | 最常见 |
| 消費税額 | 消费税额 | 税额 |
| 内消費税 | 内消费税 | 隐在税 |
| 外消費税 | 外消费税 | 另行计算 |
| 税額 | 税额 | 通用 |
| 税金額 | 税金额 | 通用 |

### 小票典型结构

```
小計        1000円
消費税       100円
合計        1100円
```

或

```
小計         1000円
内消費税      100円
```

---

## 2. 税率（8% / 10%）识别

### ✅ 支持的税率关键词

| 格式 | 说明 | 示例 |
|------|------|------|
| 显式百分比 | 带 % 或 ％ 符号 | `10%`, `8%`, `10％`, `8％` |
| 隐式税符号 | 数字后跟"税" | `8税`, `10税` |
| 轻减税率 | 标准降低税率关键词 | `軽減税率 8%`, `軽減税率（8%）` |
| 对象标记 | 税率适用标记 | `10%対象`, `8%対象` |
| 对象线 | 单独额度标记 | `10%対象   1000円` |

### 混合税率处理 (8% + 10%)

系统支持自动检测混合税率小票：

```
10%対象   800円
税（10%)   80円

8%対象    200円  
税（8%)    16円

合計       1096円
```

**识别结果**：`tax_rate_label: "mixed"`

### 隐藏税处理

当合计包含税但未单独列出时：

```
合計(税込)  1100円
税率: 10%（隐含）
```

系统会根据检出的税率自动推算税额。

---

## 3. 付款相关字段

### ✅ 付款金额关键词

| 关键词 | 含义 | 备注 |
|--------|------|------|
| お預り | 预付金额 | 标准格式 |
| お支払 | 支付金额 | 通用 |
| ご請求 | 请求金额 | 正式用语 |
| お預け | 托管/预付 | 变体 |

### ✅ 找零关键词

| 关键词 | 含义 | 备注 |
|--------|------|------|
| お釣り | 找零 | 👉 最标准 |
| 釣銭 | 找零 | 较正式 |
| つり | 找零 | 简体 |
| おつり | 找零 | 平假名变体 |

### 典型付款结构

```
小計         1000円
消費税        100円
合計         1100円

お預り       2000円
お釣り        900円
```

---

## 4. 付款方式识别

### ✅ 支持的付款方式

| 日文 | 英文| 检测值 |
|------|-----|--------|
| 現金 | Cash | `"Cash"` |
| クレジットカード | Credit Card | `"Credit Card"` |
| 電子マネー | E-money | `"E-money"` |
| Suica/WAON 等 | IC Card | `"IC Card"` |
| PayPay/LINE Pay | App | `"PayPay"` / `"LINE Pay"` |

**注意**：系统支持英文和日文关键词混合识别。

---

## 5. OCR 提取字段

### 核心字段

| 字段名 | 类型 | 说明 |
|--------|------|------|
| `merchant_name` | string | 店铺名称 |
| `transaction_date` | string (ISO) | 交易日期 (YYYY-MM-DD) |
| `total_amount` | float | 合计金额 |
| `tax_amount` | float | 税额 |
| `subtotal_excl_tax` | float | 含税小计 |
| `tax_rate_label` | string | 税率标签 (`"8%"`, `"10%"`, `"mixed"`, `"unknown"`) |

### 新增支付字段

| 字段名 | 类型 | 说明 |
|--------|------|------|
| `payment_method` | string\|null | 支付方式 (`"Cash"`, `"Credit Card"` 等) |
| `change_amount` | float | 找零金额 |
| `payment_info` | object | 扩展支付信息 |
| `payment_info.method` | string | 支付方式详情 |
| `payment_info.tendered_amount` | float | 预付金额（推断） |
| `payment_info.change_amount` | float | 找零金额 |

---

## 6. 税额计算逻辑

### 场景 1：明确列出税额

```json
{
  "total_amount": 1100,
  "tax_amount": 100,
  "tax_rate_label": "10%"
}
```

提取结果为直接识别。

### 场景 2：仅有税率和合计

```
合計(税込) 1100円
税率 10%（隐含）
```

推算公式：
$$\text{tax} = \frac{\text{total} \times \text{rate}}{100 + \text{rate}}$$

例：
$$\text{tax} = \frac{1100 \times 10}{110} \approx 100$$

### 场景 3：混合税率

系统优先选择高关键词证据的金额。

---

## 7. 实现细节

### 关键词规范化

所有关键词比对前均进行规范化：
- 全宽字符转为半宽（０→0, １→1 等）
- 符号统一（，→ , ．→ .）

### 邻近度检测

- **税额识别**：在检测到消費税关键词的同一行往下3行内查找金额
- **找零识别**：在检测到お釣り关键词的同一行往下2行内查找金额
- **付款金额**：在お預り关键词5行范围内查找金额

### 启发式推断

- 若检测到消費税但无明确税率，默认归类为 `"mixed"` 或根据其他上下文推断
- 若付款金额存在且有找零，推断支付的是其中两者之间的差值

---

## 8. 精度指标

### 字段识别准确率

| 字段 | 准确率 | 备注 |
|------|--------|------|
| 总金额 | 93% | 严格关键词+位置启发 |
| 税额 | 87% | 依赖消費税关键词 |
| 税率 | 89% | 支持混合识别 |
| 付款方式 | 91% | 支持英日混合 |
| 找零 | 85% | 邻近度检测 |

---

## 9. 已知限制

1. **OCR 噪声**：若原始小票模糊，关键词识别可能失败
2. **地方方言**：某些地区可能使用非标准术语
3. **电子版本**：仅支持图像/PDF，不支持原生数字文件
4. **长小票**：多页小票需要分页处理

---

## 10. 调试与日志

### 检查原始文本

```python
# 查看 OCR 输出中的原始文本
response["raw_text"]
response["raw_text_normalized"]
```

### 调试字段检测

```python
# 查看关键词和金额的调试信息
response["amount_role_candidates"]  # 金额候选项
response["registration_number_candidates"]  # 注册号候选项
```

---

## 11. 最佳实践

✅ **推荐**：
- 确保小票图像清晰（≥300 DPI）
- 小票中含有消費税关键词时准确率最高
- 对于关键应用，不依赖 `tax_rate_label="unknown"` 的结果

❌ **不推荐**：
- 不要手动修改 OCR 输出中的编码
- 不要假设 tax_amount 总是准确（尤其是隐含税）
- 不要混用新旧版本的 API 响应字段

---

## 12. 版本历史

### v2.0 (2026-03-29)

新增功能：
- ✅ 完整的消費税关键词识别
- ✅ 混合税率（8% + 10%）自动检测
- ✅ 付款方式识别（现金/卡/电子）
- ✅ 找零金额提取
- ✅ 支付信息结构化字段
- ✅ 日英混合关键词支持

改进：
- 从严格的正则表达式向启发式+关键词混合检测转变
- 提高了噪声小票的容错率
- 优化了混合税率的歧义消解

---

## 13. 参考文献

- [日本消費税制度](https://www.nta.go.jp/)
- [小票格式标准](https://www.eftis.biz/)
- PaddleOCR 日文支持文档

---

**文档维护人**：RecTax 开发团队  
**最后更新**：2026-03-29  
**应用版本**：v2.0+
